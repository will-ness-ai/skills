# The code-story reference files are an example and a template, not code.
# Without this line they are 57 KB of HTML, and GitHub calls this an HTML repo.
skills/*/references/*.html linguist-documentation

# A skill is a Markdown document, so Markdown is the language of this repo.
# GitHub does not count Markdown unless you ask it to.
*.md linguist-detectable=true
